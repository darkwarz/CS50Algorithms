#include <cs50.h>
#include <stdio.h>
#include <math.h>

int main(void)
{
    int input = get_int("Enter number: ");
    printf("%i\n", input);
}

{
    if (n === 0) { return 1; }
    ans = (n * i(n-1));

  return ans;

}